/*
 * Note: this file originally auto-generated by mib2c using
 *        : mib2c.scalar.conf 17337 2009-01-01 14:28:29Z magfr $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "commonDeviceNum.h"
#include "../product_common/snmp_ipc_interfaceTC.h"

/** Initializes the commonDeviceNum module */
void
init_commonDeviceNum(void)
{
    const oid       commonDeviceNum_oid[] =
        { 1, 3, 6, 1, 4, 1, 17409, 1, 3, 3, 2, 1 };

    DEBUGMSGTL(("commonDeviceNum", "Initializing\n"));

    netsnmp_register_scalar(netsnmp_create_handler_registration
                            ("commonDeviceNum", handle_commonDeviceNum,
                             commonDeviceNum_oid,
                             OID_LENGTH(commonDeviceNum_oid),
                             HANDLER_CAN_RWRITE));
}

int
handle_commonDeviceNum(netsnmp_mib_handler *handler,
                       netsnmp_handler_registration *reginfo,
                       netsnmp_agent_request_info *reqinfo,
                       netsnmp_request_info *requests)
{
    int             ret;
	unsigned long commonDeviceNum = 1;
    /*
     * We are never called for a GETNEXT if it's registered as a
     * "instance", as it's "magically" handled for us.  
     */

    /*
     * a instance handler also only hands us one request at a time, so
     * we don't need to loop over a list of requests; we'll only get one. 
     */
	#if 0
	unsigned short dev_status_update = 0;
	catvinfo_t *info = NULL;
	catvinfo_t info_send;
	ipc_catvinfo_ack_t *pack = NULL;
	ipc_catvinfo_ack_t *pack_tmp = NULL;

	pack_tmp = ipc_catv_config(ipc_fd, IPC_CONFIG_GET, IPC_APPLY_NONE, NULL);
	if(pack_tmp && pack_tmp->hdr.status == IPC_STATUS_OK) 
	{		
		info = &pack_tmp->catvinfo;
		output_level_atten = info->output_level_atten;
		dev_status_update = info->dev_status_update;
		if(pack_tmp)
		{
			free(pack_tmp);
		}
	}
	else
	{	if(pack_tmp)
		{		
			free(pack_tmp);
		}
		snmp_log(LOG_ERR, "snmp get comDeviceRFAttenuation from master fail!\n");
		return SNMP_ERR_GENERR;
	}
#endif
    switch (reqinfo->mode) {

    case MODE_GET:
        snmp_set_var_typed_value(requests->requestvb, ASN_INTEGER,
                                 (u_char *)&commonDeviceNum
                                 /* XXX: a pointer to the scalar's data */
                                 ,sizeof(&commonDeviceNum)
                                 /*
                                  * XXX: the length of the data in bytes 
                                  */ );
        break;

        /*
         * SET REQUEST
         *
         * multiple states in the transaction.  See:
         * http://www.net-snmp.org/tutorial-5/toolkit/mib_module/set-actions.jpg
         */
    case MODE_SET_RESERVE1:
        /*
         * or you could use netsnmp_check_vb_type_and_size instead 
         */
        ret = netsnmp_check_vb_type(requests->requestvb, ASN_INTEGER);
        if (ret != SNMP_ERR_NOERROR) {
            netsnmp_set_request_error(reqinfo, requests, ret);
        }
        break;

    case MODE_SET_RESERVE2:
        /*
         * XXX malloc "undo" storage buffer 
         */
         #if 0
        if ( /* XXX if malloc, or whatever, failed: */ ) {
            netsnmp_set_request_error(reqinfo, requests,
                                      SNMP_ERR_RESOURCEUNAVAILABLE);
        }
		#endif
        break;

    case MODE_SET_FREE:
        /*
         * XXX: free resources allocated in RESERVE1 and/or
         * RESERVE2.  Something failed somewhere, and the states
         * below won't be called. 
         */
        break;

    case MODE_SET_ACTION:
        /*
         * XXX: perform the value change here 
         */
        if ( requests->requestvb->val.integer ) 
        {
			;
		}
		else
		{
            netsnmp_set_request_error(reqinfo, requests, /* some error */SNMP_ERR_BADVALUE
                                      );
        }
        break;

    case MODE_SET_COMMIT:
        /*
         * XXX: delete temporary storage 
         */
         #if 0
        if ( /* XXX: error? */ ) {
            /*
             * try _really_really_ hard to never get to this point 
             */
            netsnmp_set_request_error(reqinfo, requests,
                                      SNMP_ERR_COMMITFAILED);
        }
		#endif
        break;

    case MODE_SET_UNDO:
        /*
         * XXX: UNDO and return to previous value for the object 
         */
         #if 0
        if ( /* XXX: error? */ ) {
            /*
             * try _really_really_ hard to never get to this point 
             */
            netsnmp_set_request_error(reqinfo, requests,
                                      SNMP_ERR_UNDOFAILED);
        }
		#endif
        break;

    default:
        /*
         * we should never get here, so this is a really bad error 
         */
        snmp_log(LOG_ERR, "unknown mode (%d) in handle_commonDeviceNum\n",
                 reqinfo->mode);
        return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}
