/*
 * Note: this file originally auto-generated by mib2c using
 *        : mib2c.scalar.conf 17337 2009-01-01 14:28:29Z magfr $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "zgSystemInfo.h"
#include "../product_common/snmp_ipc_interfaceTC.h"

/** Initializes the zgSystemInfo module */
void
init_zgSystemInfo(void)
{
    const oid       zgsystemGUIType_oid[] =
        { 1, 3, 6, 1, 4, 1, 39596, 3, 3 };

    DEBUGMSGTL(("zgSystemInfo", "Initializing\n"));

    netsnmp_register_scalar(netsnmp_create_handler_registration
                            ("zgsystemGUIType", handle_zgsystemGUIType,
                             zgsystemGUIType_oid,
                             OID_LENGTH(zgsystemGUIType_oid),
                             HANDLER_CAN_RONLY));
}

int
handle_zgsystemGUIType(netsnmp_mib_handler *handler,
                       netsnmp_handler_registration *reginfo,
                       netsnmp_agent_request_info *reqinfo,
                       netsnmp_request_info *requests)
{
    /*
     * We are never called for a GETNEXT if it's registered as a
     * "instance", as it's "magically" handled for us.  
     */

    /*
     * a instance handler also only hands us one request at a time, so
     * we don't need to loop over a list of requests; we'll only get one. 
     */
	char			zgsystemGUIType[MAX_SYSTEMINFO_SIZE];
	memset(zgsystemGUIType, 0, sizeof(zgsystemGUIType));
	memcpy(zgsystemGUIType, "Other", sizeof(zgsystemGUIType));
    switch (reqinfo->mode) {

    case MODE_GET:
        snmp_set_var_typed_value(requests->requestvb, ASN_OCTET_STR,
                                 (u_char *)zgsystemGUIType
                                 /* XXX: a pointer to the scalar's data */
                                 ,strlen(zgsystemGUIType)
                                 /*
                                  * XXX: the length of the data in bytes 
                                  */ );
        break;


    default:
        /*
         * we should never get here, so this is a really bad error 
         */
        snmp_log(LOG_ERR, "unknown mode (%d) in handle_zgsystemGUIType\n",
                 reqinfo->mode);
        return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}
