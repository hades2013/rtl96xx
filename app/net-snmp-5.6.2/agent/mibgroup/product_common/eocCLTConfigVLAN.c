/*
 * Note: this file originally auto-generated by mib2c using
 *        : mib2c.scalar.conf 11805 2005-01-07 09:37:18Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "eocCLTConfigVLAN.h"

#include "ipc_interfaceTC.h"

#undef DBGID
#define DBGID  "hc:cltvlan"

#ifndef DBGID
#define DBGID "defalut"
#endif 

static switch_vlan_mode_t vlan_mode;

static long cltConfigVLANMode = 0;

LOCAL_COMMIT_DECLARE();

#define local_commit()  LOCAL_COMMIT(0, cltConfigVLANMode_commit, NULL)


/** Initializes the eocCLTConfigVLAN module */
void
init_eocCLTConfigVLAN(void)
{
    static oid      cltConfigVLANMode_oid[] =
        { 1, 3, 6, 1, 4, 1, 36368, 1, 1, 1, 1, 2, 3, 2 };
    static oid      cltConfigVLANTotalNumber_oid[] =
        { 1, 3, 6, 1, 4, 1, 36368, 1, 1, 1, 1, 2, 3, 3 };

    DEBUGMSGTL((DBGID, "Initializing\n"));

    netsnmp_register_scalar(netsnmp_create_handler_registration
                            ("cltConfigVLANMode", handle_cltConfigVLANMode,
                             cltConfigVLANMode_oid,
                             OID_LENGTH(cltConfigVLANMode_oid),
                             HANDLER_CAN_RWRITE));
    netsnmp_register_scalar(netsnmp_create_handler_registration
                            ("cltConfigVLANTotalNumber",
                             handle_cltConfigVLANTotalNumber,
                             cltConfigVLANTotalNumber_oid,
                             OID_LENGTH(cltConfigVLANTotalNumber_oid),
                             HANDLER_CAN_RONLY));
}

int
handle_cltConfigVLANMode(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *reqinfo,
                         netsnmp_request_info *requests)
{
    int             ret;
    /*
     * We are never called for a GETNEXT if it's registered as a
     * "instance", as it's "magically" handled for us.  
     */

    /*
     * a instance handler also only hands us one request at a time, so
     * we don't need to loop over a list of requests; we'll only get one. 
     */

    switch (reqinfo->mode) {

    case MODE_GET:
		cltConfigVLANMode_load(NULL, NULL);
        snmp_set_var_typed_value(requests->requestvb, ASN_INTEGER,
                                 (u_char *)&cltConfigVLANMode
                                 /* XXX: a pointer to the scalar's data */
                                 ,
                                 sizeof(cltConfigVLANMode)/*
                                  * XXX: the length of the data in bytes 
                                  */ );
        break;

        /*
         * SET REQUEST
         *
         * multiple states in the transaction.  See:
         * http://www.net-snmp.org/tutorial-5/toolkit/mib_module/set-actions.jpg
         */
    case MODE_SET_RESERVE1:
        /*
         * or you could use netsnmp_check_vb_type_and_size instead 
         */
        ret = netsnmp_check_vb_type(requests->requestvb, ASN_INTEGER);
        if (ret != SNMP_ERR_NOERROR) {
            netsnmp_set_request_error(reqinfo, requests, ret);
        }
        if (!validVLANMode(vb_int(requests->requestvb))) {
            netsnmp_set_request_error(reqinfo, requests, SNMP_ERR_WRONGVALUE);
        }
		
        break;

    case MODE_SET_RESERVE2:
        /*
         * XXX malloc "undo" storage buffer 
         */
		
        break;

    case MODE_SET_FREE:
        /*
         * XXX: free resources allocated in RESERVE1 and/or
         * RESERVE2.  Something failed somewhere, and the states
         * below won't be called. 
         */
        break;

    case MODE_SET_ACTION:
        /*
         * XXX: perform the value change here 
         */
		cltConfigVLANMode = vb_int(requests->requestvb);
        break;

    case MODE_SET_COMMIT:
        /*
         * XXX: delete temporary storage 
         */
		local_commit();
        break;

    case MODE_SET_UNDO:
        /*
         * XXX: UNDO and return to previous value for the object 
         */
        break;

    default:
        /*
         * we should never get here, so this is a really bad error 
         */
        snmp_log(LOG_ERR,
                 "unknown mode (%d) in handle_cltConfigVLANMode\n",
                 reqinfo->mode);
        return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}

int
handle_cltConfigVLANTotalNumber(netsnmp_mib_handler *handler,
                                netsnmp_handler_registration *reginfo,
                                netsnmp_agent_request_info *reqinfo,
                                netsnmp_request_info *requests)
{
	int ret;
	static long value = 0;
    /*
     * We are never called for a GETNEXT if it's registered as a
     * "instance", as it's "magically" handled for us.  
     */

    /*
     * a instance handler also only hands us one request at a time, so
     * we don't need to loop over a list of requests; we'll only get one. 
     */

    switch (reqinfo->mode) {

    case MODE_GET:
		ret = vlan_update();
		value = dot1q_vlans_num;
        snmp_set_var_typed_value(requests->requestvb, ASN_INTEGER,
                                 (u_char *)&value
                                 /* XXX: a pointer to the scalar's data */
                                 ,
                                 sizeof(value)/*
                                  * XXX: the length of the data in bytes 
                                  */ );
		HC_IPC_ASSERT(ret);		
        break;


    default:
        /*
         * we should never get here, so this is a really bad error 
         */
        snmp_log(LOG_ERR,
                 "unknown mode (%d) in handle_cltConfigVLANTotalNumber\n",
                 reqinfo->mode);
        return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}

int cltConfigVLANMode_load(netsnmp_cache *cache, void *vmagic)
{
	int ret;
	ret = ipc_get_vlan_mode(&vlan_mode);

	if (HC_IPC_ERROR(ret)) {
		CLR_VALID();
		DEBUGMSGTL((DBGID, "Failed to update cltTemplateTable Group\n"));
	} else {
		SET_VALID();
		DEBUGMSGTL((DBGID, "Updated cltTemplateTable Group\n"));
	}
	
	HC_IPC_ASSERT(ret);	

	cltConfigVLANMode = (vlan_mode.mode == VLAN_MODE_8021Q) ? VLANMode_dot1q : VLANMode_transparent;
	return 0;
}

void cltConfigVLANMode_free(netsnmp_cache *cache, void *vmagic)
{

}


void cltConfigVLANMode_commit(unsigned int regNo, void *clientargs)
{
	int ret;

	HC_VALID_ASSERT();

	// reset all port config to zero	
	memset(&vlan_mode, 0, sizeof(vlan_mode));

	vlan_mode.mode = (cltConfigVLANMode == VLANMode_dot1q) ? VLAN_MODE_8021Q : VLAN_MODE_TRANSPARENT;
		
	ret = ipc_set_vlan_mode(&vlan_mode);

	if (HC_IPC_ERROR(ret)) {
		DEBUGMSGTL((DBGID, "Failed to set cltConfigVLANMode\n"));
	} else {
		DEBUGMSGTL((DBGID, "Updated cltConfigVLANMode\n"));
	}
	
	HC_IPC_ASSERT1(ret);	
}


