/*
 * Note: this file originally auto-generated by mib2c using
 *        : mib2c.scalar.conf 11805 2005-01-07 09:37:18Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "cltConfigAdministrator.h"


#include "ipc_interfaceTC.h"

#ifndef DBGID
#define DBGID  "hc:cltadmin"
#endif 

#ifndef DBGID
#define DBGID "defalut"
#endif 


LOCAL_COMMIT_DECLARE();

#define local_commit()  LOCAL_COMMIT(500, cltConfigAdministratorLoginTimeout_commit, NULL)

static sys_admin_t cltadmin;


/** Initializes the cltConfigAdministrator module */
void
init_cltConfigAdministrator(void)
{
    netsnmp_handler_registration *reginfo;
    static oid      cltConfigAdministratorLoginTimeout_oid[] =
        { 1, 3, 6, 1, 4, 1, 36368, 1, 1, 1, 1, 2, 1, 5, 1 };

	DEBUGMSGTL((DBGID, "Initializing\n"));

	reginfo = netsnmp_create_handler_registration
                            ("cltConfigAdministratorLoginTimeout",
                             handle_cltConfigAdministratorLoginTimeout,
                             cltConfigAdministratorLoginTimeout_oid,
                             OID_LENGTH
                             (cltConfigAdministratorLoginTimeout_oid),
                             HANDLER_CAN_RWRITE);
                             
    netsnmp_register_scalar(reginfo);
	
    netsnmp_inject_handler( reginfo,
		    netsnmp_get_cache_handler(  HC_CACHE_TIMEOUT,
			   		cltConfigAdministratorLoginTimeout_load, cltConfigAdministratorLoginTimeout_free,
					cltConfigAdministratorLoginTimeout_oid, OID_LENGTH(cltConfigAdministratorLoginTimeout_oid)));
	
}

int
handle_cltConfigAdministratorLoginTimeout(netsnmp_mib_handler *handler,
                                          netsnmp_handler_registration
                                          *reginfo,
                                          netsnmp_agent_request_info
                                          *reqinfo,
                                          netsnmp_request_info *requests)
{
    int             ret;
	static int value = 0;
    /*
     * We are never called for a GETNEXT if it's registered as a
     * "instance", as it's "magically" handled for us.  
     */

    /*
     * a instance handler also only hands us one request at a time, so
     * we don't need to loop over a list of requests; we'll only get one. 
     */

    switch (reqinfo->mode) {

    case MODE_GET:
		value = cltadmin.idle_time;
        snmp_set_var_typed_value(requests->requestvb, ASN_INTEGER,
                                 (u_char *)&value
                                 /* XXX: a pointer to the scalar's data */
                                 ,
                                 sizeof(value)/*
                                  * XXX: the length of the data in bytes 
                                  */ );
        break;

        /*
         * SET REQUEST
         *
         * multiple states in the transaction.  See:
         * http://www.net-snmp.org/tutorial-5/toolkit/mib_module/set-actions.jpg
         */
    case MODE_SET_RESERVE1:
        /*
         * or you could use netsnmp_check_vb_type_and_size instead 
         */
        ret = netsnmp_check_vb_type(requests->requestvb, ASN_INTEGER);
        if (ret != SNMP_ERR_NOERROR) {
            netsnmp_set_request_error(reqinfo, requests, ret);
			break;
		}

        ret = netsnmp_check_vb_range(requests->requestvb, MIN_ADMIN_TIMEOUT, MAX_ADMIN_TIMEOUT);
        if (ret != SNMP_ERR_NOERROR) {
            netsnmp_set_request_error(reqinfo, requests, ret);
			break;
		}		
        break;

    case MODE_SET_RESERVE2:
        /*
         * XXX malloc "undo" storage buffer 
         */

        break;

    case MODE_SET_FREE:
        /*
         * XXX: free resources allocated in RESERVE1 and/or
         * RESERVE2.  Something failed somewhere, and the states
         * below won't be called. 
         */
        break;

    case MODE_SET_ACTION:
        /*
         * XXX: perform the value change here 
         */
	   cltadmin.idle_time = vb_int(requests->requestvb);
        break;

    case MODE_SET_COMMIT:
        /*
         * XXX: delete temporary storage 
         */
		local_commit();
        break;

    case MODE_SET_UNDO:
        /*
         * XXX: UNDO and return to previous value for the object 
         */
        break;

    default:
        /*
         * we should never get here, so this is a really bad error 
         */
        snmp_log(LOG_ERR,
                 "unknown mode (%d) in handle_cltConfigAdministratorLoginTimeout\n",
                 reqinfo->mode);
        return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}

int cltConfigAdministratorLoginTimeout_load(netsnmp_cache *cache, void *vmagic)
{
    long            ret_value = -1;

    ret_value = ipc_get_sys_admin(&cltadmin);

    if (HC_IPC_ERROR(ret_value)) {
		CLR_VALID();
        DEBUGMSGTL((DBGID, "Failed to load cltConfigAdministratorLoginTimeout\n"));
    } else {
		SET_VALID();    	
        DEBUGMSGTL((DBGID, "Loaded cltConfigAdministratorLoginTimeout\n"));
    }

	HC_IPC_ASSERT(ret_value);
	
    return ret_value;
}


void cltConfigAdministratorLoginTimeout_free(netsnmp_cache *cache, void *magic)
{

}


void cltConfigAdministratorLoginTimeout_commit(unsigned int regNo, void *clientargs)
{
	
    long   ret_value = -1;

	HC_VALID_ASSERT();

	cltadmin.count = 0; // only set timeout

	ret_value = ipc_set_sys_admin(&cltadmin);
	
    if (HC_IPC_ERROR(ret_value)) {
        DEBUGMSGTL((DBGID, "Failed to commit cltConfigAdministratorLoginTimeout\n"));
    } else {
        DEBUGMSGTL((DBGID, "Committed cltConfigAdministratorLoginTimeout\n"));
    }

	HC_IPC_ASSERT1(ret_value);	
}

