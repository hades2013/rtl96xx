/*
 * Note: this file originally auto-generated by mib2c using
 *  : mib2c.iterate.conf 15999 2007-03-25 22:32:02Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "modEoCVLANTable.h"

/** Initializes the modEoCVLANTable module */
void
init_modEoCVLANTable(void)
{
    /*
     * here we initialize all the tables we're planning on supporting 
     */
    initialize_table_modEoCVLANTable();
}

# Determine the first/last column names

/** Initialize the modEoCVLANTable table by defining its contents and how it's structured */
void
initialize_table_modEoCVLANTable(void)
{
    static oid      modEoCVLANTable_oid[] =
        { 1, 3, 6, 1, 4, 1, 17409, 2, 4, 9, 7 };
    size_t          modEoCVLANTable_oid_len =
        OID_LENGTH(modEoCVLANTable_oid);
    netsnmp_handler_registration *reg;
    netsnmp_iterator_info *iinfo;
    netsnmp_table_registration_info *table_info;

    reg =
        netsnmp_create_handler_registration("modEoCVLANTable",
                                            modEoCVLANTable_handler,
                                            modEoCVLANTable_oid,
                                            modEoCVLANTable_oid_len,
                                            HANDLER_CAN_RWRITE);

    table_info = SNMP_MALLOC_TYPEDEF(netsnmp_table_registration_info);
    netsnmp_table_helper_add_indexes(table_info, ASN_INTEGER,   /* index: modEoCVLANIndex */
                                     0);
    table_info->min_column = COLUMN_MODEOCVLANINDEX;
    table_info->max_column = COLUMN_MODEOCVLANROWSTATUS;

    iinfo = SNMP_MALLOC_TYPEDEF(netsnmp_iterator_info);
    iinfo->get_first_data_point = modEoCVLANTable_get_first_data_point;
    iinfo->get_next_data_point = modEoCVLANTable_get_next_data_point;
    iinfo->table_reginfo = table_info;

    netsnmp_register_table_iterator(reg, iinfo);

    /*
     * Initialise the contents of the table here 
     */
}

    /*
     * Typical data structure for a row entry 
     */
struct modEoCVLANTable_entry {
    /*
     * Index values 
     */
    long            modEoCVLANIndex;

    /*
     * Column values 
     */
    long            modEoCVLANIndex;
    char            modEoCVLANName[NNN];
    size_t          modEoCVLANName_len;
    char            old_modEoCVLANName[NNN];
    size_t          old_modEoCVLANName_len;
    long            modEoCVLANFlag;
    long            modEoCVLANRowStatus;

    /*
     * Illustrate using a simple linked list 
     */
    int             valid;
    struct modEoCVLANTable_entry *next;
};

struct modEoCVLANTable_entry *modEoCVLANTable_head;

/*
 * create a new row in the (unsorted) table 
 */
struct modEoCVLANTable_entry *
modEoCVLANTable_createEntry(long modEoCVLANIndex,)
{
    struct modEoCVLANTable_entry *entry;

    entry = SNMP_MALLOC_TYPEDEF(struct modEoCVLANTable_entry);
    if (!entry)
        return NULL;

    entry->modEoCVLANIndex = modEoCVLANIndex;
    entry->next = modEoCVLANTable_head;
    modEoCVLANTable_head = entry;
    return entry;
}

/*
 * remove a row from the table 
 */
void
modEoCVLANTable_removeEntry(struct modEoCVLANTable_entry *entry)
{
    struct modEoCVLANTable_entry *ptr, *prev;

    if (!entry)
        return;                 /* Nothing to remove */

    for (ptr = modEoCVLANTable_head, prev = NULL;
         ptr != NULL; prev = ptr, ptr = ptr->next) {
        if (ptr == entry)
            break;
    }
    if (!ptr)
        return;                 /* Can't find it */

    if (prev == NULL)
        modEoCVLANTable_head = ptr->next;
    else
        prev->next = ptr->next;

    SNMP_FREE(entry);           /* XXX - release any other internal resources */
}


/*
 * Example iterator hook routines - using 'get_next' to do most of the work 
 */
netsnmp_variable_list *
modEoCVLANTable_get_first_data_point(void **my_loop_context,
                                     void **my_data_context,
                                     netsnmp_variable_list *
                                     put_index_data,
                                     netsnmp_iterator_info *mydata)
{
    *my_loop_context = modEoCVLANTable_head;
    return modEoCVLANTable_get_next_data_point(my_loop_context,
                                               my_data_context,
                                               put_index_data, mydata);
}

netsnmp_variable_list *
modEoCVLANTable_get_next_data_point(void **my_loop_context,
                                    void **my_data_context,
                                    netsnmp_variable_list * put_index_data,
                                    netsnmp_iterator_info *mydata)
{
    struct modEoCVLANTable_entry *entry =
        (struct modEoCVLANTable_entry *) *my_loop_context;
    netsnmp_variable_list *idx = put_index_data;

    if (entry) {
        snmp_set_var_typed_integer(idx, ASN_INTEGER,
                                   entry->modEoCVLANIndex);
        idx = idx->next_variable;
        *my_data_context = (void *) entry;
        *my_loop_context = (void *) entry->next;
        return put_index_data;
    } else {
        return NULL;
    }
}


/** handles requests for the modEoCVLANTable table */
int
modEoCVLANTable_handler(netsnmp_mib_handler *handler,
                        netsnmp_handler_registration *reginfo,
                        netsnmp_agent_request_info *reqinfo,
                        netsnmp_request_info *requests)
{

    netsnmp_request_info *request;
    netsnmp_table_request_info *table_info;
    struct modEoCVLANTable_entry *table_entry;

    switch (reqinfo->mode) {
        /*
         * Read-support (also covers GetNext requests)
         */
    case MODE_GET:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCVLANTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCVLANINDEX:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->modEoCVLANIndex);
                break;
            case COLUMN_MODEOCVLANNAME:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_value(request->requestvb, ASN_OCTET_STR,
                                         (u_char *) table_entry->
                                         modEoCVLANName,
                                         table_entry->modEoCVLANName_len);
                break;
            case COLUMN_MODEOCVLANFLAG:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->modEoCVLANFlag);
                break;
            case COLUMN_MODEOCVLANROWSTATUS:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCVLANRowStatus);
                break;
            default:
                netsnmp_set_request_error(reqinfo, request,
                                          SNMP_NOSUCHOBJECT);
                break;
            }
        }
        break;

        /*
         * Write-support
         */
    case MODE_SET_RESERVE1:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCVLANTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCVLANNAME:
                /*
                 * or possiblc 'netsnmp_check_vb_type_and_size' 
                 */
                ret =
                    netsnmp_check_vb_type_and_max_size(request->requestvb,
                                                       ASN_OCTET_STR,
                                                       sizeof(table_entry->
                                                              modEoCVLANName));
                if (ret != SNMP_ERR_NOERROR) {
                    netsnmp_set_request_error(reqinfo, request, ret);
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_MODEOCVLANROWSTATUS:
                ret = netsnmp_check_vb_rowstatus(request->requestvb,
                                                 (table_entry ? RS_ACTIVE :
                                                  RS_NONEXISTENT));
                if (ret != SNMP_ERR_NOERROR) {
                    netsnmp_set_request_error(reqinfo, request, ret);
                    return SNMP_ERR_NOERROR;
                }
                break;
            default:
                netsnmp_set_request_error(reqinfo, request,
                                          SNMP_ERR_NOTWRITABLE);
                return SNMP_ERR_NOERROR;
            }
        }
        break;

    case MODE_SET_RESERVE2:
        break;

    case MODE_SET_FREE:
        break;

    case MODE_SET_ACTION:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCVLANTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCVLANNAME:
                memcpy(table_entry->old_modEoCVLANName,
                       table_entry->modEoCVLANName,
                       sizeof(table_entry->modEoCVLANName));
                table_entry->old_modEoCVLANName_len =
                    table_entry->modEoCVLANName_len;
                memset(table_entry->modEoCVLANName, 0,
                       sizeof(table_entry->modEoCVLANName));
                memcpy(table_entry->modEoCVLANName,
                       request->requestvb->val.string,
                       request->requestvb->val_len);
                table_entry->modEoCVLANName_len =
                    request->requestvb->val_len;
                break;
            }
        }
        /*
         * Check the internal consistency of an active row 
         */
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCVLANTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCVLANROWSTATUS:
                switch (*request->requestvb->val.integer) {
                case RS_ACTIVE:
                case RS_CREATEANDGO:
                    if ( /* XXX */ ) {
                        netsnmp_set_request_error(reqinfo, request,
                                                  SNMP_ERR_INCONSISTENTVALUE);
                        return SNMP_ERR_NOERROR;
                    }
                }
            }
        }
        break;

    case MODE_SET_UNDO:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCVLANTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCVLANNAME:
                memcpy(table_entry->modEoCVLANName,
                       table_entry->old_modEoCVLANName,
                       sizeof(table_entry->modEoCVLANName));
                memset(table_entry->old_modEoCVLANName, 0,
                       sizeof(table_entry->modEoCVLANName));
                table_entry->modEoCVLANName_len =
                    table_entry->old_modEoCVLANName_len;
                break;
            case COLUMN_MODEOCVLANROWSTATUS:
                switch (*request->requestvb->val.integer) {
                case RS_CREATEANDGO:
                case RS_CREATEANDWAIT:
                    if (table_entry && !table_entry->valid) {
                        modEoCVLANTable_removeEntry(table_data, table_row);
                    }
                }
                break;
            }
        }
        break;

    case MODE_SET_COMMIT:
        break;
    }
    return SNMP_ERR_NOERROR;
}
