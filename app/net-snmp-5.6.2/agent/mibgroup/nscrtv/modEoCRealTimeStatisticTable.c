/*
 * Note: this file originally auto-generated by mib2c using
 *  : mib2c.iterate.conf 15999 2007-03-25 22:32:02Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "modEoCRealTimeStatisticTable.h"

/** Initializes the modEoCRealTimeStatisticTable module */
void
init_modEoCRealTimeStatisticTable(void)
{
    /*
     * here we initialize all the tables we're planning on supporting 
     */
    initialize_table_modEoCRealTimeStatisticTable();
}

# Determine the first/last column names

/** Initialize the modEoCRealTimeStatisticTable table by defining its contents and how it's structured */
void
initialize_table_modEoCRealTimeStatisticTable(void)
{
    static oid      modEoCRealTimeStatisticTable_oid[] =
        { 1, 3, 6, 1, 4, 1, 17409, 2, 4, 6, 1 };
    size_t          modEoCRealTimeStatisticTable_oid_len =
        OID_LENGTH(modEoCRealTimeStatisticTable_oid);
    netsnmp_handler_registration *reg;
    netsnmp_iterator_info *iinfo;
    netsnmp_table_registration_info *table_info;

    reg =
        netsnmp_create_handler_registration("modEoCRealTimeStatisticTable",
                                            modEoCRealTimeStatisticTable_handler,
                                            modEoCRealTimeStatisticTable_oid,
                                            modEoCRealTimeStatisticTable_oid_len,
                                            HANDLER_CAN_RWRITE);

    table_info = SNMP_MALLOC_TYPEDEF(netsnmp_table_registration_info);
    netsnmp_table_helper_add_indexes(table_info, ASN_INTEGER,   /* index: modEoCRealTimeStatisticCBATCardIndex */
                                     ASN_INTEGER,       /* index: modEoCRealTimeStatisticCNUIndex */
                                     0);
    table_info->min_column = COLUMN_MODEOCREALTIMESTATISTICCBATCARDINDEX;
    table_info->max_column = COLUMN_MODEOCREALTIMESTATISTICRESET;

    iinfo = SNMP_MALLOC_TYPEDEF(netsnmp_iterator_info);
    iinfo->get_first_data_point =
        modEoCRealTimeStatisticTable_get_first_data_point;
    iinfo->get_next_data_point =
        modEoCRealTimeStatisticTable_get_next_data_point;
    iinfo->table_reginfo = table_info;

    netsnmp_register_table_iterator(reg, iinfo);

    /*
     * Initialise the contents of the table here 
     */
}

    /*
     * Typical data structure for a row entry 
     */
struct modEoCRealTimeStatisticTable_entry {
    /*
     * Index values 
     */
    long            modEoCRealTimeStatisticCBATCardIndex;
    long            modEoCRealTimeStatisticCNUIndex;

    /*
     * Column values 
     */
    long            modEoCRealTimeStatisticCBATCardIndex;
    long            modEoCRealTimeStatisticCNUIndex;
    U64             modEoCRealTimeStatisticTxByte;
    U64             modEoCRealTimeStatisticRxByte;
    long            modEoCRealTimeStatisticTxPkt;
    long            modEoCRealTimeStatisticRxPkt;
    long            modEoCRealTimeStatisticTxBcast;
    long            modEoCRealTimeStatisticRxBcast;
    long            modEoCRealTimeStatisticTxMcast;
    long            modEoCRealTimeStatisticRxMcast;
    long            modEoCRealTimeStatisticRxCrc;
    long            modEoCRealTimeStatisticTxShort;
    long            modEoCRealTimeStatisticRxShort;
    long            modEoCRealTimeStatisticTxDropped;
    long            modEoCRealTimeStatisticRxDropped;
    long            modEoCRealTimeStatisticAveragePreFEC;
    long            modEoCRealTimeStatisticReset;
    long            old_modEoCRealTimeStatisticReset;

    /*
     * Illustrate using a simple linked list 
     */
    int             valid;
    struct modEoCRealTimeStatisticTable_entry *next;
};

struct modEoCRealTimeStatisticTable_entry
    *modEoCRealTimeStatisticTable_head;

/*
 * create a new row in the (unsorted) table 
 */
struct modEoCRealTimeStatisticTable_entry *
modEoCRealTimeStatisticTable_createEntry(long
                                         modEoCRealTimeStatisticCBATCardIndex,
                                         long
                                         modEoCRealTimeStatisticCNUIndex,)
{
    struct modEoCRealTimeStatisticTable_entry *entry;

    entry = SNMP_MALLOC_TYPEDEF(struct modEoCRealTimeStatisticTable_entry);
    if (!entry)
        return NULL;

    entry->modEoCRealTimeStatisticCBATCardIndex =
        modEoCRealTimeStatisticCBATCardIndex;
    entry->modEoCRealTimeStatisticCNUIndex =
        modEoCRealTimeStatisticCNUIndex;
    entry->next = modEoCRealTimeStatisticTable_head;
    modEoCRealTimeStatisticTable_head = entry;
    return entry;
}

/*
 * remove a row from the table 
 */
void
modEoCRealTimeStatisticTable_removeEntry(struct
                                         modEoCRealTimeStatisticTable_entry
                                         *entry)
{
    struct modEoCRealTimeStatisticTable_entry *ptr, *prev;

    if (!entry)
        return;                 /* Nothing to remove */

    for (ptr = modEoCRealTimeStatisticTable_head, prev = NULL;
         ptr != NULL; prev = ptr, ptr = ptr->next) {
        if (ptr == entry)
            break;
    }
    if (!ptr)
        return;                 /* Can't find it */

    if (prev == NULL)
        modEoCRealTimeStatisticTable_head = ptr->next;
    else
        prev->next = ptr->next;

    SNMP_FREE(entry);           /* XXX - release any other internal resources */
}


/*
 * Example iterator hook routines - using 'get_next' to do most of the work 
 */
netsnmp_variable_list *
modEoCRealTimeStatisticTable_get_first_data_point(void **my_loop_context,
                                                  void **my_data_context,
                                                  netsnmp_variable_list *
                                                  put_index_data,
                                                  netsnmp_iterator_info
                                                  *mydata)
{
    *my_loop_context = modEoCRealTimeStatisticTable_head;
    return
        modEoCRealTimeStatisticTable_get_next_data_point(my_loop_context,
                                                         my_data_context,
                                                         put_index_data,
                                                         mydata);
}

netsnmp_variable_list *
modEoCRealTimeStatisticTable_get_next_data_point(void **my_loop_context,
                                                 void **my_data_context,
                                                 netsnmp_variable_list *
                                                 put_index_data,
                                                 netsnmp_iterator_info
                                                 *mydata)
{
    struct modEoCRealTimeStatisticTable_entry *entry =
        (struct modEoCRealTimeStatisticTable_entry *) *my_loop_context;
    netsnmp_variable_list *idx = put_index_data;

    if (entry) {
        snmp_set_var_typed_integer(idx, ASN_INTEGER,
                                   entry->
                                   modEoCRealTimeStatisticCBATCardIndex);
        idx = idx->next_variable;
        snmp_set_var_typed_integer(idx, ASN_INTEGER,
                                   entry->modEoCRealTimeStatisticCNUIndex);
        idx = idx->next_variable;
        *my_data_context = (void *) entry;
        *my_loop_context = (void *) entry->next;
        return put_index_data;
    } else {
        return NULL;
    }
}


/** handles requests for the modEoCRealTimeStatisticTable table */
int
modEoCRealTimeStatisticTable_handler(netsnmp_mib_handler *handler,
                                     netsnmp_handler_registration *reginfo,
                                     netsnmp_agent_request_info *reqinfo,
                                     netsnmp_request_info *requests)
{

    netsnmp_request_info *request;
    netsnmp_table_request_info *table_info;
    struct modEoCRealTimeStatisticTable_entry *table_entry;

    switch (reqinfo->mode) {
        /*
         * Read-support (also covers GetNext requests)
         */
    case MODE_GET:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCRealTimeStatisticTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCREALTIMESTATISTICCBATCARDINDEX:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCRealTimeStatisticCBATCardIndex);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICCNUINDEX:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCRealTimeStatisticCNUIndex);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICTXBYTE:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb,
                                           ASN_COUNTER64,
                                           table_entry->
                                           modEoCRealTimeStatisticTxByte);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICRXBYTE:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb,
                                           ASN_COUNTER64,
                                           table_entry->
                                           modEoCRealTimeStatisticRxByte);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICTXPKT:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCRealTimeStatisticTxPkt);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICRXPKT:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCRealTimeStatisticRxPkt);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICTXBCAST:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCRealTimeStatisticTxBcast);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICRXBCAST:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCRealTimeStatisticRxBcast);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICTXMCAST:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCRealTimeStatisticTxMcast);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICRXMCAST:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCRealTimeStatisticRxMcast);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICRXCRC:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCRealTimeStatisticRxCrc);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICTXSHORT:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCRealTimeStatisticTxShort);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICRXSHORT:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCRealTimeStatisticRxShort);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICTXDROPPED:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCRealTimeStatisticTxDropped);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICRXDROPPED:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCRealTimeStatisticRxDropped);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICAVERAGEPREFEC:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCRealTimeStatisticAveragePreFEC);
                break;
            case COLUMN_MODEOCREALTIMESTATISTICRESET:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCRealTimeStatisticReset);
                break;
            default:
                netsnmp_set_request_error(reqinfo, request,
                                          SNMP_NOSUCHOBJECT);
                break;
            }
        }
        break;

        /*
         * Write-support
         */
    case MODE_SET_RESERVE1:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCRealTimeStatisticTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCREALTIMESTATISTICRESET:
                /*
                 * or possibly 'netsnmp_check_vb_int_range' 
                 */
                ret = netsnmp_check_vb_int(request->requestvb);
                if (ret != SNMP_ERR_NOERROR) {
                    netsnmp_set_request_error(reqinfo, request, ret);
                    return SNMP_ERR_NOERROR;
                }
                break;
            default:
                netsnmp_set_request_error(reqinfo, request,
                                          SNMP_ERR_NOTWRITABLE);
                return SNMP_ERR_NOERROR;
            }
        }
        break;

    case MODE_SET_RESERVE2:
        break;

    case MODE_SET_FREE:
        break;

    case MODE_SET_ACTION:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCRealTimeStatisticTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCREALTIMESTATISTICRESET:
                table_entry->old_modEoCRealTimeStatisticReset =
                    table_entry->modEoCRealTimeStatisticReset;
                table_entry->modEoCRealTimeStatisticReset =
                    *request->requestvb->val.integer;
                break;
            }
        }
        break;

    case MODE_SET_UNDO:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCRealTimeStatisticTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCREALTIMESTATISTICRESET:
                table_entry->modEoCRealTimeStatisticReset =
                    table_entry->old_modEoCRealTimeStatisticReset;
                table_entry->old_modEoCRealTimeStatisticReset = 0;
                break;
            }
        }
        break;

    case MODE_SET_COMMIT:
        break;
    }
    return SNMP_ERR_NOERROR;
}
