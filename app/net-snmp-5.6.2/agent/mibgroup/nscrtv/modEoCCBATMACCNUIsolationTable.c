/*
 * Note: this file originally auto-generated by mib2c using
 *  : mib2c.iterate.conf 15999 2007-03-25 22:32:02Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "modEoCCBATMACCNUIsolationTable.h"

#include "nscrtv.h"

#ifndef DBGID
#define DBGID  "modeoccnuiso"
#endif 


/** Initializes the modEoCCBATMACCNUIsolationTable module */
void
init_modEoCCBATMACCNUIsolationTable(void)
{
    /*
     * here we initialize all the tables we're planning on supporting 
     */
    initialize_table_modEoCCBATMACCNUIsolationTable();
}

struct modEoCCBATMACCNUIsolationTable_entry *
modEoCCBATMACCNUIsolationTable_createEntry(long
                                           modEoCCBATMACCNUIsolationIndex, 
                                           long enable);
                                           

//# Determine the first/last column names

/** Initialize the modEoCCBATMACCNUIsolationTable table by defining its contents and how it's structured */
void
initialize_table_modEoCCBATMACCNUIsolationTable(void)
{
    static oid      modEoCCBATMACCNUIsolationTable_oid[] =
        { 1, 3, 6, 1, 4, 1, 17409, 2, 4, 3, 1 };
    size_t          modEoCCBATMACCNUIsolationTable_oid_len =
        OID_LENGTH(modEoCCBATMACCNUIsolationTable_oid);
    netsnmp_handler_registration *reg;
    netsnmp_iterator_info *iinfo;
    netsnmp_table_registration_info *table_info;
	int i;

    reg =
        netsnmp_create_handler_registration
        ("modEoCCBATMACCNUIsolationTable",
         modEoCCBATMACCNUIsolationTable_handler,
         modEoCCBATMACCNUIsolationTable_oid,
         modEoCCBATMACCNUIsolationTable_oid_len, HANDLER_CAN_RWRITE);

    table_info = SNMP_MALLOC_TYPEDEF(netsnmp_table_registration_info);
    netsnmp_table_helper_add_indexes(table_info, ASN_INTEGER,   /* index: modEoCCBATMACCNUIsolationIndex */
                                     0);
    table_info->min_column = COLUMN_MODEOCCBATMACCNUISOLATIONINDEX;
    table_info->max_column = COLUMN_MODEOCCBATMACCNUISOLATIONEN;

    iinfo = SNMP_MALLOC_TYPEDEF(netsnmp_iterator_info);
    iinfo->get_first_data_point =
        modEoCCBATMACCNUIsolationTable_get_first_data_point;
    iinfo->get_next_data_point =
        modEoCCBATMACCNUIsolationTable_get_next_data_point;
    iinfo->table_reginfo = table_info;

    netsnmp_register_table_iterator(reg, iinfo);

    /*
     * Initialise the contents of the table here 
     */
    for (i = 0; i < MAX_CLT_CHANNEL; i ++){
		modEoCCBATMACCNUIsolationTable_createEntry(i, 1);	  
	}
	
}

    /*
     * Typical data structure for a row entry 
     */
struct modEoCCBATMACCNUIsolationTable_entry {
    /*
     * Index values 
     */
    long            modEoCCBATMACCNUIsolationIndex;

    /*
     * Column values 
     */
//    long            modEoCCBATMACCNUIsolationIndex;
    long            modEoCCBATMACCNUIsolationEN;
    long            old_modEoCCBATMACCNUIsolationEN;

    /*
     * Illustrate using a simple linked list 
     */
    int             valid;
    struct modEoCCBATMACCNUIsolationTable_entry *next;
};

struct modEoCCBATMACCNUIsolationTable_entry
    *modEoCCBATMACCNUIsolationTable_head;

/*
 * create a new row in the (unsorted) table 
 */
struct modEoCCBATMACCNUIsolationTable_entry *
modEoCCBATMACCNUIsolationTable_createEntry(long
                                           modEoCCBATMACCNUIsolationIndex, 
                                           long enable)
{
    struct modEoCCBATMACCNUIsolationTable_entry *entry;

    entry =
        SNMP_MALLOC_TYPEDEF(struct modEoCCBATMACCNUIsolationTable_entry);
    if (!entry)
        return NULL;

	entry->modEoCCBATMACCNUIsolationEN = enable;
    entry->modEoCCBATMACCNUIsolationIndex = modEoCCBATMACCNUIsolationIndex;
    entry->next = modEoCCBATMACCNUIsolationTable_head;
    modEoCCBATMACCNUIsolationTable_head = entry;
    return entry;
}

/*
 * remove a row from the table 
 */
void
modEoCCBATMACCNUIsolationTable_removeEntry(struct
                                           modEoCCBATMACCNUIsolationTable_entry
                                           *entry)
{
    struct modEoCCBATMACCNUIsolationTable_entry *ptr, *prev;

    if (!entry)
        return;                 /* Nothing to remove */

    for (ptr = modEoCCBATMACCNUIsolationTable_head, prev = NULL;
         ptr != NULL; prev = ptr, ptr = ptr->next) {
        if (ptr == entry)
            break;
    }
    if (!ptr)
        return;                 /* Can't find it */

    if (prev == NULL)
        modEoCCBATMACCNUIsolationTable_head = ptr->next;
    else
        prev->next = ptr->next;

    SNMP_FREE(entry);           /* XXX - release any other internal resources */
}


/*
 * Example iterator hook routines - using 'get_next' to do most of the work 
 */
netsnmp_variable_list *
modEoCCBATMACCNUIsolationTable_get_first_data_point(void **my_loop_context,
                                                    void **my_data_context,
                                                    netsnmp_variable_list *
                                                    put_index_data,
                                                    netsnmp_iterator_info
                                                    *mydata)
{
    *my_loop_context = modEoCCBATMACCNUIsolationTable_head;
    return
        modEoCCBATMACCNUIsolationTable_get_next_data_point(my_loop_context,
                                                           my_data_context,
                                                           put_index_data,
                                                           mydata);
}

netsnmp_variable_list *
modEoCCBATMACCNUIsolationTable_get_next_data_point(void **my_loop_context,
                                                   void **my_data_context,
                                                   netsnmp_variable_list *
                                                   put_index_data,
                                                   netsnmp_iterator_info
                                                   *mydata)
{
    struct modEoCCBATMACCNUIsolationTable_entry *entry =
        (struct modEoCCBATMACCNUIsolationTable_entry *) *my_loop_context;
    netsnmp_variable_list *idx = put_index_data;

    if (entry) {
        snmp_set_var_typed_integer(idx, ASN_INTEGER,
                                   entry->modEoCCBATMACCNUIsolationIndex);
        idx = idx->next_variable;
        *my_data_context = (void *) entry;
        *my_loop_context = (void *) entry->next;
        return put_index_data;
    } else {
        return NULL;
    }
}


/** handles requests for the modEoCCBATMACCNUIsolationTable table */
int
modEoCCBATMACCNUIsolationTable_handler(netsnmp_mib_handler *handler,
                                       netsnmp_handler_registration
                                       *reginfo,
                                       netsnmp_agent_request_info *reqinfo,
                                       netsnmp_request_info *requests)
{

    netsnmp_request_info *request;
    netsnmp_table_request_info *table_info;
    struct modEoCCBATMACCNUIsolationTable_entry *table_entry;

    switch (reqinfo->mode) {
        /*
         * Read-support (also covers GetNext requests)
         */
    case MODE_GET:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCCBATMACCNUIsolationTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCCBATMACCNUISOLATIONINDEX:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCCBATMACCNUIsolationIndex);
                break;
            case COLUMN_MODEOCCBATMACCNUISOLATIONEN:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCCBATMACCNUIsolationEN);
                break;
            default:
                netsnmp_set_request_error(reqinfo, request,
                                          SNMP_NOSUCHOBJECT);
                break;
            }
        }
        break;

        /*
         * Write-support
         */
    case MODE_SET_RESERVE1:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCCBATMACCNUIsolationTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCCBATMACCNUISOLATIONEN:
                /*
                 * or possibly 'netsnmp_check_vb_int_range' 
                 */
                ret = netsnmp_check_vb_int(request->requestvb);
                if (ret != SNMP_ERR_NOERROR) {
                    netsnmp_set_request_error(reqinfo, request, ret);
                    return SNMP_ERR_NOERROR;
                }
                break;
            default:
                netsnmp_set_request_error(reqinfo, request,
                                          SNMP_ERR_NOTWRITABLE);
                return SNMP_ERR_NOERROR;
            }
        }
        break;

    case MODE_SET_RESERVE2:
        break;

    case MODE_SET_FREE:
        break;

    case MODE_SET_ACTION:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCCBATMACCNUIsolationTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCCBATMACCNUISOLATIONEN:
                table_entry->old_modEoCCBATMACCNUIsolationEN =
                    table_entry->modEoCCBATMACCNUIsolationEN;
                table_entry->modEoCCBATMACCNUIsolationEN =
                    *request->requestvb->val.integer;
                break;
            }
        }
        break;

    case MODE_SET_UNDO:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCCBATMACCNUIsolationTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCCBATMACCNUISOLATIONEN:
                table_entry->modEoCCBATMACCNUIsolationEN =
                    table_entry->old_modEoCCBATMACCNUIsolationEN;
                table_entry->old_modEoCCBATMACCNUIsolationEN = 0;
                break;
            }
        }
        break;

    case MODE_SET_COMMIT:
        break;
    }
    return SNMP_ERR_NOERROR;
}
