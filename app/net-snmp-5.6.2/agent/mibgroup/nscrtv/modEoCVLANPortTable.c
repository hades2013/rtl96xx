/*
 * Note: this file originally auto-generated by mib2c using
 *  : mib2c.iterate.conf 15999 2007-03-25 22:32:02Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "modEoCVLANPortTable.h"

/** Initializes the modEoCVLANPortTable module */
void
init_modEoCVLANPortTable(void)
{
    /*
     * here we initialize all the tables we're planning on supporting 
     */
    initialize_table_modEoCVLANPortTable();
}

# Determine the first/last column names

/** Initialize the modEoCVLANPortTable table by defining its contents and how it's structured */
void
initialize_table_modEoCVLANPortTable(void)
{
    static oid      modEoCVLANPortTable_oid[] =
        { 1, 3, 6, 1, 4, 1, 17409, 2, 4, 9, 8 };
    size_t          modEoCVLANPortTable_oid_len =
        OID_LENGTH(modEoCVLANPortTable_oid);
    netsnmp_handler_registration *reg;
    netsnmp_iterator_info *iinfo;
    netsnmp_table_registration_info *table_info;

    reg =
        netsnmp_create_handler_registration("modEoCVLANPortTable",
                                            modEoCVLANPortTable_handler,
                                            modEoCVLANPortTable_oid,
                                            modEoCVLANPortTable_oid_len,
                                            HANDLER_CAN_RWRITE);

    table_info = SNMP_MALLOC_TYPEDEF(netsnmp_table_registration_info);
    netsnmp_table_helper_add_indexes(table_info, ASN_INTEGER,   /* index: modEoCVLANCBATCardIndex */
                                     0);
    table_info->min_column = COLUMN_MODEOCVLANCBATCARDINDEX;
    table_info->max_column = COLUMN_MODEOCVLANPORTMODE;

    iinfo = SNMP_MALLOC_TYPEDEF(netsnmp_iterator_info);
    iinfo->get_first_data_point = modEoCVLANPortTable_get_first_data_point;
    iinfo->get_next_data_point = modEoCVLANPortTable_get_next_data_point;
    iinfo->table_reginfo = table_info;

    netsnmp_register_table_iterator(reg, iinfo);

    /*
     * Initialise the contents of the table here 
     */
}

    /*
     * Typical data structure for a row entry 
     */
struct modEoCVLANPortTable_entry {
    /*
     * Index values 
     */
    long            modEoCVLANCBATCardIndex;

    /*
     * Column values 
     */
    long            modEoCVLANCBATCardIndex;
    long            modEoCVLANCNUIndex;
    long            modEoCVLANPortIndex;
    long            old_modEoCVLANPortIndex;
    long            modEoCVLANPortPVID;
    long            old_modEoCVLANPortPVID;
    long            modEoCVLANPortTPID;
    long            old_modEoCVLANPortTPID;
    long            modEoCVLANPortPrio;
    long            old_modEoCVLANPortPrio;
    char            modEoCVLANPortVIDList[NNN];
    size_t          modEoCVLANPortVIDList_len;
    char            old_modEoCVLANPortVIDList[NNN];
    size_t          old_modEoCVLANPortVIDList_len;
    char            modEoCVLANPortUntaggedVIDList[NNN];
    size_t          modEoCVLANPortUntaggedVIDList_len;
    char            old_modEoCVLANPortUntaggedVIDList[NNN];
    size_t          old_modEoCVLANPortUntaggedVIDList_len;
    long            modEoCVLANPortMode;
    long            old_modEoCVLANPortMode;

    /*
     * Illustrate using a simple linked list 
     */
    int             valid;
    struct modEoCVLANPortTable_entry *next;
};

struct modEoCVLANPortTable_entry *modEoCVLANPortTable_head;

/*
 * create a new row in the (unsorted) table 
 */
struct modEoCVLANPortTable_entry *
modEoCVLANPortTable_createEntry(long modEoCVLANCBATCardIndex,)
{
    struct modEoCVLANPortTable_entry *entry;

    entry = SNMP_MALLOC_TYPEDEF(struct modEoCVLANPortTable_entry);
    if (!entry)
        return NULL;

    entry->modEoCVLANCBATCardIndex = modEoCVLANCBATCardIndex;
    entry->next = modEoCVLANPortTable_head;
    modEoCVLANPortTable_head = entry;
    return entry;
}

/*
 * remove a row from the table 
 */
void
modEoCVLANPortTable_removeEntry(struct modEoCVLANPortTable_entry *entry)
{
    struct modEoCVLANPortTable_entry *ptr, *prev;

    if (!entry)
        return;                 /* Nothing to remove */

    for (ptr = modEoCVLANPortTable_head, prev = NULL;
         ptr != NULL; prev = ptr, ptr = ptr->next) {
        if (ptr == entry)
            break;
    }
    if (!ptr)
        return;                 /* Can't find it */

    if (prev == NULL)
        modEoCVLANPortTable_head = ptr->next;
    else
        prev->next = ptr->next;

    SNMP_FREE(entry);           /* XXX - release any other internal resources */
}


/*
 * Example iterator hook routines - using 'get_next' to do most of the work 
 */
netsnmp_variable_list *
modEoCVLANPortTable_get_first_data_point(void **my_loop_context,
                                         void **my_data_context,
                                         netsnmp_variable_list *
                                         put_index_data,
                                         netsnmp_iterator_info *mydata)
{
    *my_loop_context = modEoCVLANPortTable_head;
    return modEoCVLANPortTable_get_next_data_point(my_loop_context,
                                                   my_data_context,
                                                   put_index_data, mydata);
}

netsnmp_variable_list *
modEoCVLANPortTable_get_next_data_point(void **my_loop_context,
                                        void **my_data_context,
                                        netsnmp_variable_list *
                                        put_index_data,
                                        netsnmp_iterator_info *mydata)
{
    struct modEoCVLANPortTable_entry *entry =
        (struct modEoCVLANPortTable_entry *) *my_loop_context;
    netsnmp_variable_list *idx = put_index_data;

    if (entry) {
        snmp_set_var_typed_integer(idx, ASN_INTEGER,
                                   entry->modEoCVLANCBATCardIndex);
        idx = idx->next_variable;
        *my_data_context = (void *) entry;
        *my_loop_context = (void *) entry->next;
        return put_index_data;
    } else {
        return NULL;
    }
}


/** handles requests for the modEoCVLANPortTable table */
int
modEoCVLANPortTable_handler(netsnmp_mib_handler *handler,
                            netsnmp_handler_registration *reginfo,
                            netsnmp_agent_request_info *reqinfo,
                            netsnmp_request_info *requests)
{

    netsnmp_request_info *request;
    netsnmp_table_request_info *table_info;
    struct modEoCVLANPortTable_entry *table_entry;

    switch (reqinfo->mode) {
        /*
         * Read-support (also covers GetNext requests)
         */
    case MODE_GET:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCVLANPortTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCVLANCBATCARDINDEX:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCVLANCBATCardIndex);
                break;
            case COLUMN_MODEOCVLANCNUINDEX:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCVLANCNUIndex);
                break;
            case COLUMN_MODEOCVLANPORTINDEX:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCVLANPortIndex);
                break;
            case COLUMN_MODEOCVLANPORTPVID:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCVLANPortPVID);
                break;
            case COLUMN_MODEOCVLANPORTTPID:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCVLANPortTPID);
                break;
            case COLUMN_MODEOCVLANPORTPRIO:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCVLANPortPrio);
                break;
            case COLUMN_MODEOCVLANPORTVIDLIST:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_value(request->requestvb, ASN_OCTET_STR,
                                         (u_char *) table_entry->
                                         modEoCVLANPortVIDList,
                                         table_entry->
                                         modEoCVLANPortVIDList_len);
                break;
            case COLUMN_MODEOCVLANPORTUNTAGGEDVIDLIST:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_value(request->requestvb, ASN_OCTET_STR,
                                         (u_char *) table_entry->
                                         modEoCVLANPortUntaggedVIDList,
                                         table_entry->
                                         modEoCVLANPortUntaggedVIDList_len);
                break;
            case COLUMN_MODEOCVLANPORTMODE:
                if (!table_entry) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer(request->requestvb, ASN_INTEGER,
                                           table_entry->
                                           modEoCVLANPortMode);
                break;
            default:
                netsnmp_set_request_error(reqinfo, request,
                                          SNMP_NOSUCHOBJECT);
                break;
            }
        }
        break;

        /*
         * Write-support
         */
    case MODE_SET_RESERVE1:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCVLANPortTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCVLANPORTINDEX:
                /*
                 * or possibly 'netsnmp_check_vb_int_range' 
                 */
                ret = netsnmp_check_vb_int(request->requestvb);
                if (ret != SNMP_ERR_NOERROR) {
                    netsnmp_set_request_error(reqinfo, request, ret);
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_MODEOCVLANPORTPVID:
                /*
                 * or possibly 'netsnmp_check_vb_int_range' 
                 */
                ret = netsnmp_check_vb_int(request->requestvb);
                if (ret != SNMP_ERR_NOERROR) {
                    netsnmp_set_request_error(reqinfo, request, ret);
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_MODEOCVLANPORTTPID:
                /*
                 * or possibly 'netsnmp_check_vb_int_range' 
                 */
                ret = netsnmp_check_vb_int(request->requestvb);
                if (ret != SNMP_ERR_NOERROR) {
                    netsnmp_set_request_error(reqinfo, request, ret);
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_MODEOCVLANPORTPRIO:
                /*
                 * or possibly 'netsnmp_check_vb_int_range' 
                 */
                ret = netsnmp_check_vb_int(request->requestvb);
                if (ret != SNMP_ERR_NOERROR) {
                    netsnmp_set_request_error(reqinfo, request, ret);
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_MODEOCVLANPORTVIDLIST:
                /*
                 * or possiblc 'netsnmp_check_vb_type_and_size' 
                 */
                ret =
                    netsnmp_check_vb_type_and_max_size(request->requestvb,
                                                       ASN_OCTET_STR,
                                                       sizeof(table_entry->
                                                              modEoCVLANPortVIDList));
                if (ret != SNMP_ERR_NOERROR) {
                    netsnmp_set_request_error(reqinfo, request, ret);
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_MODEOCVLANPORTUNTAGGEDVIDLIST:
                /*
                 * or possiblc 'netsnmp_check_vb_type_and_size' 
                 */
                ret =
                    netsnmp_check_vb_type_and_max_size(request->requestvb,
                                                       ASN_OCTET_STR,
                                                       sizeof(table_entry->
                                                              modEoCVLANPortUntaggedVIDList));
                if (ret != SNMP_ERR_NOERROR) {
                    netsnmp_set_request_error(reqinfo, request, ret);
                    return SNMP_ERR_NOERROR;
                }
                break;
            case COLUMN_MODEOCVLANPORTMODE:
                /*
                 * or possibly 'netsnmp_check_vb_int_range' 
                 */
                ret = netsnmp_check_vb_int(request->requestvb);
                if (ret != SNMP_ERR_NOERROR) {
                    netsnmp_set_request_error(reqinfo, request, ret);
                    return SNMP_ERR_NOERROR;
                }
                break;
            default:
                netsnmp_set_request_error(reqinfo, request,
                                          SNMP_ERR_NOTWRITABLE);
                return SNMP_ERR_NOERROR;
            }
        }
        break;

    case MODE_SET_RESERVE2:
        break;

    case MODE_SET_FREE:
        break;

    case MODE_SET_ACTION:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCVLANPortTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCVLANPORTINDEX:
                table_entry->old_modEoCVLANPortIndex =
                    table_entry->modEoCVLANPortIndex;
                table_entry->modEoCVLANPortIndex =
                    *request->requestvb->val.integer;
                break;
            case COLUMN_MODEOCVLANPORTPVID:
                table_entry->old_modEoCVLANPortPVID =
                    table_entry->modEoCVLANPortPVID;
                table_entry->modEoCVLANPortPVID =
                    *request->requestvb->val.integer;
                break;
            case COLUMN_MODEOCVLANPORTTPID:
                table_entry->old_modEoCVLANPortTPID =
                    table_entry->modEoCVLANPortTPID;
                table_entry->modEoCVLANPortTPID =
                    *request->requestvb->val.integer;
                break;
            case COLUMN_MODEOCVLANPORTPRIO:
                table_entry->old_modEoCVLANPortPrio =
                    table_entry->modEoCVLANPortPrio;
                table_entry->modEoCVLANPortPrio =
                    *request->requestvb->val.integer;
                break;
            case COLUMN_MODEOCVLANPORTVIDLIST:
                memcpy(table_entry->old_modEoCVLANPortVIDList,
                       table_entry->modEoCVLANPortVIDList,
                       sizeof(table_entry->modEoCVLANPortVIDList));
                table_entry->old_modEoCVLANPortVIDList_len =
                    table_entry->modEoCVLANPortVIDList_len;
                memset(table_entry->modEoCVLANPortVIDList, 0,
                       sizeof(table_entry->modEoCVLANPortVIDList));
                memcpy(table_entry->modEoCVLANPortVIDList,
                       request->requestvb->val.string,
                       request->requestvb->val_len);
                table_entry->modEoCVLANPortVIDList_len =
                    request->requestvb->val_len;
                break;
            case COLUMN_MODEOCVLANPORTUNTAGGEDVIDLIST:
                memcpy(table_entry->old_modEoCVLANPortUntaggedVIDList,
                       table_entry->modEoCVLANPortUntaggedVIDList,
                       sizeof(table_entry->modEoCVLANPortUntaggedVIDList));
                table_entry->old_modEoCVLANPortUntaggedVIDList_len =
                    table_entry->modEoCVLANPortUntaggedVIDList_len;
                memset(table_entry->modEoCVLANPortUntaggedVIDList, 0,
                       sizeof(table_entry->modEoCVLANPortUntaggedVIDList));
                memcpy(table_entry->modEoCVLANPortUntaggedVIDList,
                       request->requestvb->val.string,
                       request->requestvb->val_len);
                table_entry->modEoCVLANPortUntaggedVIDList_len =
                    request->requestvb->val_len;
                break;
            case COLUMN_MODEOCVLANPORTMODE:
                table_entry->old_modEoCVLANPortMode =
                    table_entry->modEoCVLANPortMode;
                table_entry->modEoCVLANPortMode =
                    *request->requestvb->val.integer;
                break;
            }
        }
        break;

    case MODE_SET_UNDO:
        for (request = requests; request; request = request->next) {
            table_entry = (struct modEoCVLANPortTable_entry *)
                netsnmp_extract_iterator_context(request);
            table_info = netsnmp_extract_table_info(request);

            switch (table_info->colnum) {
            case COLUMN_MODEOCVLANPORTINDEX:
                table_entry->modEoCVLANPortIndex =
                    table_entry->old_modEoCVLANPortIndex;
                table_entry->old_modEoCVLANPortIndex = 0;
                break;
            case COLUMN_MODEOCVLANPORTPVID:
                table_entry->modEoCVLANPortPVID =
                    table_entry->old_modEoCVLANPortPVID;
                table_entry->old_modEoCVLANPortPVID = 0;
                break;
            case COLUMN_MODEOCVLANPORTTPID:
                table_entry->modEoCVLANPortTPID =
                    table_entry->old_modEoCVLANPortTPID;
                table_entry->old_modEoCVLANPortTPID = 0;
                break;
            case COLUMN_MODEOCVLANPORTPRIO:
                table_entry->modEoCVLANPortPrio =
                    table_entry->old_modEoCVLANPortPrio;
                table_entry->old_modEoCVLANPortPrio = 0;
                break;
            case COLUMN_MODEOCVLANPORTVIDLIST:
                memcpy(table_entry->modEoCVLANPortVIDList,
                       table_entry->old_modEoCVLANPortVIDList,
                       sizeof(table_entry->modEoCVLANPortVIDList));
                memset(table_entry->old_modEoCVLANPortVIDList, 0,
                       sizeof(table_entry->modEoCVLANPortVIDList));
                table_entry->modEoCVLANPortVIDList_len =
                    table_entry->old_modEoCVLANPortVIDList_len;
                break;
            case COLUMN_MODEOCVLANPORTUNTAGGEDVIDLIST:
                memcpy(table_entry->modEoCVLANPortUntaggedVIDList,
                       table_entry->old_modEoCVLANPortUntaggedVIDList,
                       sizeof(table_entry->modEoCVLANPortUntaggedVIDList));
                memset(table_entry->old_modEoCVLANPortUntaggedVIDList, 0,
                       sizeof(table_entry->modEoCVLANPortUntaggedVIDList));
                table_entry->modEoCVLANPortUntaggedVIDList_len =
                    table_entry->old_modEoCVLANPortUntaggedVIDList_len;
                break;
            case COLUMN_MODEOCVLANPORTMODE:
                table_entry->modEoCVLANPortMode =
                    table_entry->old_modEoCVLANPortMode;
                table_entry->old_modEoCVLANPortMode = 0;
                break;
            }
        }
        break;

    case MODE_SET_COMMIT:
        break;
    }
    return SNMP_ERR_NOERROR;
}
